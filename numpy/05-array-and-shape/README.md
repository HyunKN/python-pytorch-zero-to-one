# 05 — NumPy Array와 Shape

NumPy array의 구조와 통계값을 확인하는 함수를 구현합니다.

## 구현할 기능

- `shape`, `ndim`, `dtype` 확인
- 최솟값, 최댓값, 평균 계산
- 결과를 Python `dict`로 반환

## 배울 내용

- `np.ndarray`
- axis와 dimension
- dtype
- vectorized operation

## 완료 기준

- [ ] 1D와 2D array에서 동작
- [ ] 반환값의 의미를 설명
- [ ] Python list와 NumPy array 차이를 기록
