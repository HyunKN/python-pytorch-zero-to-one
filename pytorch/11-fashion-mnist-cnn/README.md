# 11 — FashionMNIST CNN

같은 FashionMNIST 문제를 CNN으로 해결하고 MLP와 결과를 비교합니다.

## 구현할 기능

- convolution, activation, pooling layer 구성
- CNN `forward()` 구현
- MLP와 동일한 조건으로 학습
- parameter 수와 accuracy 비교

## 배울 내용

- channel, height, width
- convolution kernel과 feature map
- pooling
- 모델 비교 조건

## 완료 기준

- [ ] 각 layer 출력 shape 기록
- [ ] MLP/CNN parameter 수 비교
- [ ] 같은 validation set에서 accuracy 비교
